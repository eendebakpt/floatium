name: ci

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: test / py${{ matrix.python }} / ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-14, windows-latest]
        python: ["3.11", "3.12", "3.13", "3.13t", "3.14", "3.14t", "3.15", "3.15t"]
        exclude:
          # Free-threaded macOS/Windows builds can have wheel availability
          # lag; drop them from the PR matrix but keep Linux-t for coverage.
          - os: macos-14
            python: "3.13t"
          - os: windows-latest
            python: "3.13t"
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python }}
          allow-prereleases: true

      - name: Install build deps and package
        run: |
          python -m pip install --upgrade pip
          python -m pip install ".[test]"

      - name: Show info
        run: |
          python -c "import floatium; print(floatium.info())"

      - name: Run unit + parity + edge tests
        run: |
          python -m pytest tests -x --tb=short --ignore=tests/cpython_suite

      - name: Run vendored CPython tests (if any)
        run: |
          python -m pytest tests/cpython_suite --tb=short || true
        shell: bash
